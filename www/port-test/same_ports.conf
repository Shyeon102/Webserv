# 기대:  파싱 후 checkDuplicateServer 에서 거부
#        "Startup error: Error: duplicate server: same listen address and server_name"
#        -> 서버가 뜨지 않고 종료 (exit 1)
server {
    listen 9092;
    server_name dup.local;
    root ./www;
    index index.html;
}

server {
    listen 9092;
    server_name dup.local;
    root ./www;
    index index.html;
}